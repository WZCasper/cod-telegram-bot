from datetime import datetime, timedelta

# В памяти будем хранить время последней отправки, но оно не сохраняется между запусками.
# Вместо этого используем параметр "min_interval_minutes" из state и проверяем в main.
# Здесь просто заглушка, можно не использовать.